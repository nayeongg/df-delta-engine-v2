package kr.ac.df.delta.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataSourceConfig {
    // PostgreSQL-only datasource is configured via application.yml
}
